import 'package:flutter/material.dart';

/// Paleta de colores oficial TruekApp
class AppColors {
  // Colores principales
  static const Color primary = Color(0xFF166534); // TruekApp Green
  static const Color secondary = Color(0xFF0F172A); // TruekApp Dark Blue
  static const Color background = Color(0xFFF9FAFB); // Light background

  // Colores neutrales
  static const Color neutralLight = Color(0xFFE5E7EB);
  static const Color neutralDark = Color(0xFF6B7280);

  // Estados
  static const Color errorColor = Color(0xFFDC2626);
  static const Color successColor = Color(0xFF10B981);
  static const Color warningColor = Color(0xFFF59E0B);

  // Superficies
  static const Color surface = Color(0xFFFFFFFF);
  static const Color surfaceLight = Color(0xFFFDFDFD);

  // Sombras
  static const Color shadowColor = Color(0x1A000000);

  // Transparencias
  static const Color overlayDark = Color(0x4D000000);
  static const Color overlayLight = Color(0x33FFFFFF);
}
